import styled from "styled-components";
import { BaseCellStyles } from "../styles";

export const BrownCellContainer = styled.td`
  ${BaseCellStyles}
  background-color: rgb(136, 125, 114);
`;
